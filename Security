local PlayerName = game.Players.LocalPlayer.Name
print(PlayerName)

local validNames = { "lYESIL", "abcdefyou210", "xshadow_gt" }

for _, name in ipairs(validNames) do
    if PlayerName == name then
            print("Name is on the list")
            loadstring(game:HttpGet('https://raw.githubusercontent.com/CatHead132/CatHub/main/CatHub.lua'))()
        break
    end
end
